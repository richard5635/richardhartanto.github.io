import * as React from "react";
import { Container, Header, Segment, Icon } from "semantic-ui-react";

export default () => {
  return (
    <div>
      <a href="/"><Icon className="large" name="dribble"></Icon>Go Back</a>
    </div>
  );
};
